[project]
name = "chainlit-sqlalchemy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "sqlalchemy>=2.0.28,<3",
]

[project.optional-dependencies]
azure = [
  "chainlit-azure",
]
s3 = [
  "chainlit-s3",
]

[dependency-groups]
dev = [
    "chainlit-pytest-plugin",
]

[tool.uv.sources]
chainlit-azure = { workspace = true }
chainlit-s3 = { workspace = true }
chainlit-pytest-plugin = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
